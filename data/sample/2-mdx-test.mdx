---
title: MDX test
category: sample
---

import { useState } from 'react';

**Contentlayer makes working with content easy.** It is a content preprocessor that validates and transforms your content into type-safe JSON you can easily import into your application.

![Photo by Maëva Vigier](https://images.unsplash.com/photo-1699362232821-b2eccfc516e5?q=80&w=2670&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D)

## Testing React in MDX

export const Counter = () => {
  const [count, setCount] = useState(0);

return (

<div className='flex w-full flex-col items-center gap-2'>
  <div className='flex h-20 w-20 select-none items-center justify-center rounded-lg bg-primary text-4xl font-bold text-primary-foreground'>
    {count}
  </div>
  <button
    onClick={() => setCount(prev => prev + 1)}
    className='flex h-9 cursor-default select-none flex-row items-center rounded-lg bg-white px-4 text-sm font-medium text-neutral-800 shadow-sm shadow-neutral-950/20 transition-colors duration-200 state-layer hover:state-layer-neutral-500/10 focus-visible:outline-none focus-visible:state-layer-neutral-500/10 active:bg-neutral-50 active:duration-0 dark:bg-neutral-900 dark:text-neutral-300 dark:active:bg-neutral-800'
  >
    Increment
  </button>
</div>
); };

<Counter />

### Code for this component

```tsx
import { useState } from 'react';

const Counter = () => {
  const [count, setCount] = useState(0);

  return (
    <div className='flex w-full flex-col items-center gap-2'>
      <div className='bg-primary text-primary-foreground flex h-20 w-20 select-none items-center justify-center rounded-lg text-4xl font-bold'>
        {count}
      </div>
      <button
        onClick={() => setCount(prev => prev + 1)}
        className='state-layer hover:state-layer-neutral-500/10 focus-visible:state-layer-neutral-500/10 flex h-9 cursor-default select-none flex-row items-center rounded-lg bg-white px-4 text-sm font-medium text-neutral-800 shadow-sm shadow-neutral-950/20 transition-colors duration-200 focus-visible:outline-none active:bg-neutral-50 active:duration-0 dark:bg-neutral-900 dark:text-neutral-300 dark:active:bg-neutral-800'
      >
        Increment
      </button>
    </div>
  );
};
```
