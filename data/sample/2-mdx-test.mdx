---
title: MDX test
category: sample
---

import { useState } from 'react';

**Contentlayer makes working with content easy.** It is a content preprocessor that validates and transforms your content into type-safe JSON you can easily import into your application.

![Photo by Maëva Vigier](https://images.unsplash.com/photo-1699362232821-b2eccfc516e5?q=80&w=2670&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D)

## Testing React in MDX

export const Counter = () => {
  const [count, setCount] = useState(0);

return (

<div className='flex w-full flex-col items-center gap-2'>
  <div className='flex h-20 w-20 select-none items-center justify-center rounded-lg bg-primary text-4xl font-bold text-primary-foreground'>
    {count}
  </div>
  <button
    onClick={() => setCount(prev => prev + 1)}
    className='inline-flex h-10 cursor-default items-center justify-center whitespace-nowrap rounded-md border border-input/50 bg-background px-4 text-sm font-medium shadow-sm shadow-neutral-950/10 ring-offset-background transition-[background-color,opacity] state-layer hover:state-layer-foreground/5 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 active:duration-0 active:state-layer-foreground/10 dark:bg-neutral-800 [&>*]:z-10'
  >
    Increment
  </button>
</div>
); };

<Counter />

### Code for this component

```tsx
import { useState } from 'react';

const Counter = () => {
  const [count, setCount] = useState(0);

  return (
    <div className='flex w-full flex-col items-center gap-2'>
      <div className='bg-primary text-primary-foreground flex h-20 w-20 select-none items-center justify-center rounded-lg text-4xl font-bold'>
        {count}
      </div>
      <button
        onClick={() => setCount(prev => prev + 1)}
        className='border-input/50 bg-background ring-offset-background state-layer hover:state-layer-foreground/5 focus-visible:ring-ring active:state-layer-foreground/10 inline-flex h-10 cursor-default items-center justify-center whitespace-nowrap rounded-md border px-4 text-sm font-medium shadow-sm shadow-neutral-950/10 transition-[background-color,opacity] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 active:duration-0 dark:bg-neutral-800 [&>*]:z-10'
      >
        Increment
      </button>
    </div>
  );
};
```
